//Always use same codinng conventiosn for all your js projects
//Sapace Around operators
// two spaces for indentation
//end a simple statment with a semicolon
// for compuntd statement
        // Put the opening bracket at the end of the first line.
        // Use one space before the opening bracket.
        // Put the closing bracket on a new line, without leading spaces.
        // Do not end a complex statement with a semicolon.

// object rules
        // Place the opening bracket on the same line as the object name.
        // Use colon plus one space between each property and its value.
        // Use quotes around string values, not around numeric values.
        // Do not add a comma after the last property-value pair.
        // Place the closing bracket on a new line, without leading spaces.
        // Always end an object definition with a semicolon.

// avoid line length <80

// naming conventions
    //usig same for all code eg.=>
        //Variable and function naems in camelCase
        //global variables in UPPERCASE
        //constants in uppercase

//always use lower case file names

// Avoid =>
    //global variables :in a web page global variables belong to the page can be used by all other scripts in the page, (non declared variables are also global even created inside a function)
    //new
    //==
    //eval()